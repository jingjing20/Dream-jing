// 配置独立为一个模块
const config = {
    port: 3000,
    database: {
        DATABASE: 'haohao',
        USERNAME: 'root',
        PASSWORD: '109872',
        PORT: '3306',
        HOST: '127.0.0.1'
    }
}

module.exports = config;       //向外输出