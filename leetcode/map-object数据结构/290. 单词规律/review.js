/**
 * @param {string} pattern
 * @param {string} s
 * @return {boolean}
 */
var wordPattern = function (pattern, s) {};

let pattern = 'abba',
	str = 'dog cat cat dog';

console.log(wordPattern(pattern, str)); //jing-log
